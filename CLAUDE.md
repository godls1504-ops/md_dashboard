# CLAUDE.md — 가나 스윔 MD 대시보드 프로젝트

## 프로젝트 목표

**세부 목적**: 가나 스윔의 **시즌 중간 상품 운영 성과와 재고 건전성을 진단**하고, 그 결과로 **재고 액션 검토 목록**을 만든다.

- 이 프로젝트의 산출물은 **① 진단(무엇이·왜 문제인가)** 과 **② 실행 가능한 재고 액션 검토 목록**이다. (`action_log`의 recommended_action 형태로 정리)
- **대시보드는 분석의 목적이 아니라 모니터링 구현물이다.** 분석으로 도출한 진단·지표를 지속적으로 관찰하기 위한 도구일 뿐, 대시보드를 만드는 것 자체가 목표가 아니다. 따라서 분석·진단이 먼저이고, 대시보드는 그 결과를 보여주는 화면으로 뒤따른다.

분석 기준일은 **2026-07-31** (시즌 중간 시점), 데이터 기간은 **2026-02-01 ~ 2026-07-31**, 통화는 **KRW**다.

대시보드는 다음 3가지 질문에 답할 수 있어야 한다.

### 1. 시즌이 계획대로 가고 있는가? (제품 성과)
현 시점에서 무엇이 잘 팔리고 무엇이 안 팔리는지, 남은 시즌을 어떻게 운영할지 판단할 수 있어야 한다.
- 카테고리·제품·SKU별 매출, 판매수량, **소진율(sell-through)**
- **공헌이익(기여마진)** 및 마진율 — 매출만이 아니라 실제로 남는 돈
- 신상품/스테디 제품의 판매 추세 (출시일 대비 소진 속도)
- 취소·반품이 성과를 얼마나 갉아먹고 있는가

### 2. 어떤 채널·프로모션이 효율적인가? (수익성)
- 채널별 매출·공헌이익 (수수료율이 다름: 자사몰 2.9% ~ 무신사 16%)
- 프로모션별 ROI, 할인 분담(`funded_by`: channel vs brand), 예산 소진
- 트래픽 퍼널(세션→조회→장바구니→결제)과 **ROAS**(`traffic_daily.ad_spend` 대비 귀속 매출)
- **목적**: "어디에·무엇에 돈을 더/덜 쓸지"를 근거 숫자로 제시

### 3. 재고 운영을 어떻게 조정할 것인가? (재고 건전성 · 액션)
- **재고주수(WOS)** 기준으로 리오더/클리어런스 대상 식별
- 품절 위험 SKU vs 과잉 재고 SKU
- `inventory_policy`의 카테고리·시즌별 기준(reorder/clearance point) 대비 현황
- `action_log`와 연계해 추천 액션의 실행 상태·효과 추적

---

## 작업 진행 방식 (반드시 준수)

### 규칙 1. 한 단계씩만 진행하고 멈춘다
각 단계의 분석을 마무리한 뒤 **다음 분석을 자동으로 이어서 수행하지 않는다.**

- 한 번의 응답에서 하나의 분석 단계만 수행한다
- 단계가 끝나면 결과를 보고하고 **멈춘다**
- 사용자가 명시적으로 다음 단계를 지시할 때까지 기다린다

### 규칙 2. 다음 분석은 "추천"만 한다
단계 결과 보고 뒤에는 다음에 분석할 것을 **추천**한다. 추천은 제안이지 실행 예고가 아니다.

- 추천 항목은 2~4개, 각각 "무엇을 / 왜 / 무엇을 알 수 있는지" 형태로 제시
- 추천한 분석을 먼저 진행하지 않는다
- "이어서 진행하겠습니다" 같은 표현을 쓰지 않는다

### 규칙 3. 작업 전에 계획을 먼저 알린다
파일 생성·수정, 명령 실행 전에 무엇을 할지 계획을 먼저 제시하고 확인을 받는다.

---

## 데이터 처리 규칙

### 원본은 절대 수정하지 않는다
`data/raw/ganaswim_dataset.xlsx`는 **읽기 전용**이다. 어떤 경우에도 덮어쓰지 않는다.
> 현재 이 파일은 저장소 루트에 있다. 폴더 구조를 세팅할 때 `data/raw/`로 옮기고, CLAUDE.md의 경로도 함께 유지한다.

### 한글 인코딩 규칙 (이 프로젝트의 실제 함정)
- 엑셀 원본은 한글이 포함돼 있고, 시트/컬럼명도 한글이다. **터미널(cp949)에서 그대로 출력하면 깨진다.**
- 읽기: `pandas.read_excel(path, sheet_name=...)` 또는 `openpyxl`. 시트가 19개이므로 `sheet_name=None`으로 dict 로딩 가능.
- 쓰기(CSV): **반드시 `encoding="utf-8-sig"`, `index=False`** (Excel에서 한글 안 깨짐)
- 콘솔 확인이 필요하면 결과를 UTF-8 파일로 써서 읽는다(터미널 직접 출력 지양).

### 분석 결과는 중간 산출물로 분리 저장한다
모든 분석 결과는 원본과 별개 파일로 저장한다.

| 저장 위치 | 용도 |
|---|---|
| `data/converted/` | 원본 XLSX 승인 시트를 **무손실 변환**한 CSV (값·ID 보존, 재생성 가능) |
| `data/interim/` | 정제·조인 진행 중인 중간 데이터 |
| `data/processed/` | 정제 완료, 대시보드가 읽는 최종 데이터 |
| `outputs/tables/` | 분석 단계별 집계 결과 (지표 테이블) |

`data/converted/`는 `src/convert_xlsx.py`로 생성한다. 원본 시트명·열 순서·값을 그대로 보존하며(ID는 문자열), `python -m src.convert_xlsx`로 언제든 재생성된다. 승인 대상은 `data_dictionary`에 등재된 17개 테이블(README·data_dictionary 제외)이다.

**파일 이름 규칙**: `{단계번호}_{분석내용}.csv`
- 예: `01_cleaned_orderitem.csv`, `02_sellthrough_by_product.csv`, `03_wos_by_sku.csv`

데이터 흐름은 한 방향이다:
```
data/raw (원본 xlsx, 불변)
   → data/converted (무손실 CSV 변환, 승인 17시트)
      → data/interim (조인·가공 중)
         → data/processed (최종)
            → outputs/tables (분석 결과 집계)
```

---

## 지표 정의 (데이터셋 README 기준 — 임의로 바꾸지 않는다)

분석·대시보드는 아래 정의를 **단일 기준**으로 삼는다. 계산은 `src/metrics.py`에 함수로 모아 재사용한다.

| 지표 | 정의 |
|---|---|
| **유효 매출** | `판매단가 × (수량 − canceled_qty) − 쿠폰할인액(유효수량 배부) − 완료된 반품 환불액` |
| **공헌이익(기여마진)** | 유효 매출 − (원가 + 채널수수료 + 결제수수료 + 풀필먼트비 + 포장비 + 반품 처리비) |
| **소진율(sell-through)** | 누적 순판매수량 ÷ 누적 공급(입고)수량 |
| **재고주수(WOS)** | 기준일 가용재고 ÷ (최근 28일 순판매수량 ÷ 4) |
| **반품율** | 반품수량 ÷ 판매수량 (귀책주체 `판매자/구매자` 구분해 함께 본다) |
| **ROAS** | 채널·기간별 귀속 매출 ÷ `ad_spend` |
| **전환 퍼널** | sessions → product_views → add_to_cart → checkout_start → attributed_order_credit |

**귀속(attribution) 주의**: `order_attribution`의 주문별 `attribution_weight` 합계는 1이다. `traffic_daily.attributed_order_credit`는 날짜·채널별 귀속 가중치 합계이므로, 채널 성과 배분 시 이 값을 사용한다.

**액션 판단 기준**: 재고 조정은 WOS 단독이 아니라 `inventory_policy`의 카테고리·시즌별 `clearance_point_wos`·`reorder_point_wos`와 **품절 위험**을 함께 적용한다. 단, 정책 테이블은 교육용 승인 기준이며 정답이 아니므로 임계값 민감도를 함께 검토한다.

---

## 분석 결정 사항 (확정)

아래는 확정된 프로젝트 결정으로, 분석·집계 시 예외 없이 적용한다.

1. **분석 기준일 = 2026-07-31.** 이 시점 기준으로 "시즌 중간" 성과·재고를 진단한다.
2. **기준일 이후 발생 이벤트(9건)는 "미발생"으로 간주.** `order_item.cancel_date`·`returns.처리일` 중 2026-07-31을 넘는 건은 기준일 스냅샷에 반영하지 않는다(아직 취소·반품이 일어나지 않은 것으로 처리).
3. **매출·이익 집계 단위 = `orders`.** 품목(`order_item`) 값을 주문 단위로 집계해 산출한다.
4. **6~7월 발주는 현재 "발주 중단" 상태**로 해석한다(신규 발주 없음). 재고·공급 판단 시 반영.
5. **`inventory_policy` 범위**: 유효기간이 주문 데이터 기간(~2026-07-31)을 넘어 2026-08-31까지 이어지는 정책(8월 이후 시즌 포함)도 **이번 시즌 중간 점검 범위에 포함**한다.
6. **`action_log.sku_id` 결측은 의도된 값**(상품 단위 액션)이다. 보정·추정·행 제거를 하지 않는다.
7. **`action_log`는 `product_id` 단위로 통일**해 다룬다. `sku_id` 지정 여부를 액션의 실제 적용 범위로 해석하지 않으며, SKU 단위로 분해하지 않는다(근거 없는 정밀도 방지).

---

## 설명 방식 (비개발자·MD/경영진 기준)

분석 결과는 **MD 담당자나 경영진이 읽고 바로 이해할 수 있어야** 한다.

### 반드시 포함할 것
1. **결론 먼저** — 숫자를 나열하기 전에 "그래서 무슨 뜻인지"를 한 문장으로 먼저 쓴다
2. **용어 풀이** — 분석 용어가 나오면 그 자리에서 괄호나 표로 풀어준다
   - 예: "소진율 (들여온 수량 중 지금까지 팔린 비율)"
   - 예: "공헌이익 (매출에서 원가·수수료·물류비 등 변동비를 뺀, 실제로 남는 돈)"
   - 예: "재고주수 (지금 재고가 최근 판매 속도로 몇 주치인지. 낮으면 품절 위험, 높으면 과잉 재고)"
3. **숫자의 의미** — "반품율 12%"가 아니라 "반품율 12% (100건 중 약 12건 반품)"
4. **표본 크기 경고** — 판매 건수가 적은 SKU/제품(예: 수십 건 미만)의 비율은 신뢰하기 어렵다는 점을 반드시 명시
5. **판단 근거와 한계** — 이 숫자로 말할 수 있는 것과 말할 수 없는 것을 구분

### 설명은 채팅 응답으로만 한다
자세한 설명·용어 풀이는 **채팅 응답에만 출력하고, 파일로 저장하지 않는다.**
- 파일(CSV)에는 데이터만 담는다
- 코드 주석은 개발자용 최소 수준으로만 유지한다
- 요청받지 않은 설명 문서(md 등)를 임의로 만들지 않는다

### 상관관계 ≠ 인과관계
"A 프로모션 기간에 매출이 늘었다"는 사실이고, "그 프로모션 때문이다"는 추측이다. 계절성·신상품 출시 등 다른 요인을 함께 짚고, 사실과 해석을 구분해서 표현한다.

---

## 실행 환경 (Windows)

```powershell
# 가상환경 (Python 3.14)
python -m venv .venv
.venv\Scripts\Activate.ps1

# 패키지 설치
pip install -r requirements.txt

# 분석 실행
python -m src.preprocess

# 대시보드 실행
streamlit run dashboard/app.py

# 테스트
python -m pytest tests/ -v
```

주요 패키지: streamlit, pandas, numpy, plotly, openpyxl, pyarrow, scipy, scikit-learn (정확한 버전은 `requirements.txt` 참조)

---

## 폴더 구조

```
md_dashboard/
├── data/
│   ├── raw/          # 원본 (읽기 전용) — ganaswim_dataset.xlsx
│   ├── converted/    # 원본 승인 시트의 무손실 CSV (convert_xlsx.py 산출, 재생성 가능)
│   ├── interim/      # 조인·가공 중 중간 산출물
│   └── processed/    # 최종 분석 데이터
├── notebooks/        # 탐색적 분석
├── src/
│   ├── config.py     # 경로·상수 단일 관리 (모든 경로는 여기서만 정의)
│   ├── convert_xlsx.py # 원본 XLSX → data/converted/*.csv 무손실 변환·검증
│   ├── data_loader.py # data/converted CSV 로딩·조인
│   ├── preprocess.py
│   ├── metrics.py    # MD 지표 계산 함수 (위 "지표 정의"를 구현)
│   └── viz.py
├── dashboard/
│   ├── app.py        # Streamlit 메인
│   ├── pages/
│   └── components/
├── outputs/
│   ├── figures/      # 차트 이미지
│   ├── tables/       # 분석 결과 CSV
│   └── reports/
├── logs/
└── tests/
```

### 코드 작성 원칙
- **경로는 `src/config.py`에서만 정의한다.** 상대경로 하드코딩 금지 (실행 위치가 달라도 동작해야 함)
- **지표 계산은 `src/metrics.py`에 모은다.** 대시보드에서 계산 로직을 중복 작성하지 않는다
- **대시보드는 계산하지 않는다.** `processed` 데이터를 읽고 `metrics.py`를 호출만 한다
- **대시보드는 실패해도 안내한다.** 데이터 파일이 없으면 크래시하지 않고 "먼저 `python -m src.preprocess`를 실행하세요"를 표시한다

---

## 데이터 특성 (실측 확인됨)

`data/raw/ganaswim_dataset.xlsx` — **19개 시트, 관계형 구조** (실제 거래가 아닌 교육용 **합성 데이터**)

### 시트 구성 (행 수는 데이터 행 기준)
| 구분 | 시트 (데이터 행) |
|---|---|
| 안내 | `README`, `data_dictionary`(165) |
| 마스터 | `category_master`(19), `product_master`(58), `sku_master`(399), `channel_master`(5), `warehouse_master`(2) |
| 수요 | `orders`(7,044), `order_item`(8,831), `order_attribution`(7,470), `returns`(410) |
| 공급 | `purchase_order`(741), `receipt`(741) |
| 재고 | `inventory_snapshot`(12,369), `inventory_policy`(19) |
| 프로모션 | `promotion`(4), `promotion_application`(1,300) |
| 실행 | `action_log`(32) |
| 트래픽 | `traffic_daily`(905) |

### 핵심 조인 키 (README 권장)
- `order_item.order_id` = `orders.order_id`
- `order_item.sku_id` = `sku_master.sku_id` → `product_master.product_id`
- `receipt.po_id` = `purchase_order.po_id`
- `*_master`의 `channel_id / category_id / warehouse_id`로 각 마스터 연결
- `inventory_snapshot`은 **매주 일요일 및 월말** 스냅샷 (연속 일자 아님 — 시계열 분석 시 주의)

### 반드시 처리해야 할 데이터 이슈
| 이슈 | 처리 방법 |
|---|---|
| 정상적인 공란 + 입력·연계 오류가 **공존** | 분석 전 무결성 점검(고아 키, 음수/이상치, 날짜 역전) 필수 |
| 취소(`canceled_qty`, `cancel_*`) | 매출·수량에서 반드시 차감 |
| 반품(`returns`) | 완료 건만 환불 반영, `귀책주체` 구분 |
| 문자열/날짜 형식 | `.str.strip()`·명시적 파싱, 통화·수량은 숫자형 변환 |
| 소규모 그룹 | 판매 건수 적은 SKU·소분류는 비율 해석에 경고 표시 |

### 채널 (수수료율이 성과 해석의 핵심)
| ID | 채널 | 유형 | 수수료율 |
|---|---|---|---|
| CH01 | 자사몰 | D2C | 2.9% |
| CH02 | 네이버 스마트스토어 | 마켓플레이스 | 5.8% |
| CH03 | 쿠팡 | 마켓플레이스 | 10.5% |
| CH04 | 무신사 | 패션플랫폼 | 16.0% |
| CH05 | 오프라인 성수점 | 오프라인 | 2.5% |

---

## 민감·주의 사항

- **원가·공급가·마진·거래처 정보는 대외비**다. 외부로 공유하는 산출물에는 집계 수준으로만 노출하고, 개별 거래처·원가를 그대로 게시하지 않는다.
- **정책 테이블(`inventory_policy`)은 교육용 승인 기준이며 정답이 아니다.** 임계값을 절대 기준처럼 단정하지 말고 민감도를 함께 제시한다.
- 전 데이터는 **합성(가상)** 자료다. 실제 시장·브랜드에 대한 사실처럼 일반화하지 않는다.

---

## Git 정책

- 원격: `https://github.com/godls1504-ops/md_dashboard` (기본 브랜치 `main`)
- 현재 `ganaswim_dataset.xlsx`(합성 데이터)는 저장소에 포함돼 있다. **실제 고객·거래 데이터를 추가할 경우 절대 커밋하지 않는다**(`data/`를 gitignore, 필요한 합성 원본만 예외).
- `outputs/`, `logs/`, `.venv/`, `data/converted/`, `data/interim/`, `data/processed/`는 커밋하지 않는다(모두 `data/raw` 원본에서 재생성 가능). `data/converted/`는 `python -m src.convert_xlsx`로 복원한다.
- push 전에 스테이징 목록을 확인해 대외비·개인정보가 포함되지 않았는지 검증한다.
- 커밋·push 등 원격 반영 작업은 **사용자 확인 후** 진행한다.
